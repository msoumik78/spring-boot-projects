spring:
  application:
    name: todo-app
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:demo;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

server:
  port : 8085

jwt-expiry-in-seconds: 300

